# big-text runtime dependencies. Tested with Python 3.12.3 and numpy 2.2.1, Pillow 11.0.0, PyOpenGL 3.1.9,
# glfw 2.10.1, fonttools 4.63.0, tree-sitter 0.26.0, tree-sitter-rust 0.24.2, tree-sitter-python 0.25.0,
# tree-sitter-c 0.24.2 (a fresh `pip install -r requirements.txt` on 2026-09-12); newer should work.
numpy>=1.26
Pillow>=10
PyOpenGL>=3.1.7
glfw>=2.7
fonttools>=4.50
# the resolver (atlas_resolve.py) only; the indexer, layout, history and viewer run without them
tree-sitter>=0.23
tree-sitter-rust>=0.23
tree-sitter-python>=0.23
tree-sitter-c>=0.23
