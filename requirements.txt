# big-text runtime dependencies. Tested with Python 3.12.3 and numpy 2.2.1, Pillow 11.0.0, PyOpenGL 3.1.9,
# glfw 2.10.1, fonttools 4.63.0 (a fresh `pip install -r requirements.txt` on 2026-09-12); newer should work.
numpy>=1.26
Pillow>=10
PyOpenGL>=3.1.7
glfw>=2.7
fonttools>=4.50
